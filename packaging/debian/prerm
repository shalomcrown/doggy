#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        if command -v systemctl >/dev/null 2>&1; then
            systemctl stop doggy.service || true
            systemctl stop doggy-lora.service || true
        fi
        ;;
esac

exit 0
